# Read at runtime from localStorage (see src/lib/api.ts) rather than a
# build-time env var, so you can point the same build at different
# backends (local Docker vs Cloud Run) without rebuilding. This file is
# just documentation of the default.
VITE_DEFAULT_API_BASE_URL=http://localhost:8000
